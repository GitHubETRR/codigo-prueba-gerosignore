#include <stdio.h>

int main()
{
    int dia, mes, anio;
    
    printf("Bienvenido, ingrese su fecha de nacimineto en formato DD MM AAAA:");
    scanf("%d %d %d",&dia, mes, anio);

    printf("La persona nacio el %d de %s de %d",dia,mes_palabra,anio);
    getchar();
    getchar();   
}